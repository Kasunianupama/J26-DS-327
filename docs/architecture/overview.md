# Architecture

React communicates only with FastAPI. FastAPI orchestrates independent Member 1–4 services through `shared/schemas` contracts. Member 3 is the conversational synthesis layer; Member 4 provides parallel farm and national forward-risk intelligence.

