# Research module conventions

Keep algorithms in the owning component and expose typed contracts through `shared`. Do not import another member's internal implementation. Demo services are deterministic integration placeholders, not research results.

