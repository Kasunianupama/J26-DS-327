services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: dairy_intelligence
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - dairy_intelligence_postgres:/var/lib/postgresql/data
volumes:
  dairy_intelligence_postgres:

